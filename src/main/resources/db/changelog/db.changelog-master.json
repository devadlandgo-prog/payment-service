{
  "databaseChangeLog": [
    {
      "changeSet": {
        "id": "0-create-schema",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "CREATE SCHEMA IF NOT EXISTS payments;"
            }
          }
        ],
        "runOnChange": true,
        "failOnError": false
      }
    },
    {
      "changeSet": {
        "id": "1-payment-service-schema",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO payments; CREATE TABLE IF NOT EXISTS subscriptions (id UUID PRIMARY KEY, user_id UUID NOT NULL, plan VARCHAR(50) NOT NULL, status VARCHAR(50) NOT NULL DEFAULT 'PENDING', start_date TIMESTAMP NOT NULL, end_date TIMESTAMP NOT NULL, amount DECIMAL(10,2) NOT NULL, payment_method VARCHAR(50), payment_reference VARCHAR(255), auto_renew BOOLEAN NOT NULL DEFAULT FALSE, max_vendor_views_per_month INTEGER, max_saved_lands INTEGER, can_access_premium_listings BOOLEAN NOT NULL DEFAULT FALSE, can_contact_vendor_directly BOOLEAN NOT NULL DEFAULT FALSE, cancelled_at TIMESTAMP, cancellation_reason TEXT, deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_sub_user ON subscriptions(user_id); CREATE INDEX IF NOT EXISTS idx_sub_status ON subscriptions(status);"
            }
          },
          {
            "sql": {
              "sql": "SET search_path TO payments; CREATE TABLE IF NOT EXISTS payments (id UUID PRIMARY KEY, user_id UUID NOT NULL, amount DECIMAL(10,2) NOT NULL, currency VARCHAR(3) NOT NULL DEFAULT 'CAD', status VARCHAR(20) NOT NULL DEFAULT 'PENDING', description TEXT, provider VARCHAR(50), provider_transaction_id VARCHAR(255), subscription_id UUID REFERENCES subscriptions(id), deleted BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_pay_user ON payments(user_id); CREATE INDEX IF NOT EXISTS idx_pay_sub ON payments(subscription_id);"
            }
          }
        ]
      }
    },
    {
      "changeSet": {
        "id": "2-add-subscription-plans",
        "author": "landgo",
        "changes": [
          {
            "sql": {
              "sql": "SET search_path TO payments; CREATE TABLE IF NOT EXISTS subscription_plans (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), name VARCHAR(100) NOT NULL, type VARCHAR(50) NOT NULL DEFAULT 'land_listing', description TEXT, monthly_price DECIMAL(10,2) NOT NULL DEFAULT 0.00, annual_price DECIMAL(10,2) NOT NULL DEFAULT 0.00, currency VARCHAR(3) NOT NULL DEFAULT 'CAD', features JSONB, is_popular BOOLEAN NOT NULL DEFAULT FALSE, active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_plans_type ON subscription_plans(type); CREATE INDEX IF NOT EXISTS idx_plans_active ON subscription_plans(active); INSERT INTO subscription_plans (name, type, description, monthly_price, annual_price, currency, features, is_popular) VALUES ('Free', 'land_listing', 'Basic access to browse listings', 0.00, 0.00, 'CAD', '[\"Browse listings\", \"5 vendor views/month\", \"10 saved lands\"]', FALSE), ('Basic', 'land_listing', 'Enhanced access with direct vendor contact', 9.99, 99.99, 'CAD', '[\"Everything in Free\", \"20 vendor views/month\", \"50 saved lands\", \"Direct vendor contact\"]', FALSE), ('Premium', 'land_listing', 'Full access with premium listings', 29.99, 299.99, 'CAD', '[\"Everything in Basic\", \"100 vendor views/month\", \"200 saved lands\", \"Premium listings access\"]', TRUE), ('Enterprise', 'land_listing', 'Unlimited access for professionals', 99.99, 999.99, 'CAD', '[\"Everything in Premium\", \"Unlimited vendor views\", \"Unlimited saved lands\", \"Priority support\"]', FALSE), ('Basic Pro', 'market_profession', 'Starter plan for professionals', 19.99, 199.99, 'CAD', '[\"Standard listing exposure\"]', FALSE), ('Professional', 'market_profession', 'Growth plan for active professionals', 49.99, 499.99, 'CAD', '[\"Priority placement\", \"Lead insights\"]', TRUE), ('Enterprise Pro', 'market_profession', 'Advanced plan for teams', 99.99, 999.99, 'CAD', '[\"Dedicated support\", \"Advanced analytics\"]', FALSE) ON CONFLICT DO NOTHING;"
            }
          }
        ]
      }
    }
  ]
}